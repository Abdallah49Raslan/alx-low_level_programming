#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strpbrk - writes the charcters to sdout
 * @s: the charcter to print
 * @accept: the charcter to print
 * Return: values
 */

char *_strpbrk(char *s, char *accept)
{
	return (s, accept);
}
